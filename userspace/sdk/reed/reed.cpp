/* Reed low-level graphics SDK (userspace). Talks to display via SYS_DISP_*.
 * Full API lands with gpu_display_stack; stub keeps the tree buildable. */
#include <user/sdk/reed.hpp>

namespace reed {

int create_device(void)
{
    return -1;
}

} /* namespace reed */
